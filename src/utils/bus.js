import Vue from 'vue'

/**
 *  Vue 实例
 *
 * @example
 * A组件：
 *  import Bus from '@/utils/bus'
 *  // 触发事件
 *  Bus.$emit('message', data)
 *
 *  B组件：
 *  import Bus from '@/utils/bus'
 *
 *  // 侦听事件
 *  Bus.$on('message', data => {
 *    // 收到A组件发送过来的消息
 *  })
 */
export default new Vue({})
